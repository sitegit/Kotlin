data class Key(
val field1: Int,
var field2: String
) {
var field3: String? = null
}

Могут ли возникнуть какие-то проблемы, если мы будем использовать подобный класс
в качестве ключа для HashMap?

Ответ:
Могут, так как свойство field2 мутабельно и если мы изменим это значение после добавления в HashMap,
то хэш-код тоже изменится. Соответственно после изменения мы не сможем найти этот ключ, так как индекс бакета высчитывается на основе
хэш-кода, т.е. мы не сможем прочитать или удалить ранее добавленный ключ.

